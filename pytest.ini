[pytest]
# `backend` is on the path so `import app...` resolves; `.` so `import evals...`
# does too. Lets `python -m pytest` run from the repo root with no PYTHONPATH.
pythonpath = backend .
markers =
    live_shutdown: physically stops and restores local Ollama or Sentinel Postgres (opt-in)
