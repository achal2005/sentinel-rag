# frontend/ (placeholder)

The human-approval UI lands here in **Week 3** (Safety + observability): a minimal
Next.js page that lists high-risk actions queued by the agent and lets a human
**Approve / Reject** before any n8n workflow executes.

Nothing to run yet — the Week 1 milestone is the backend RAG core (`../backend`).
